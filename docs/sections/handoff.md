# Overlevering — active contract

Last reconciled: 2026-07-13.

- Active files: `src/components/EffectBridge.tsx`, `src/styles/effect-bridge.css` and section-scoped motion in `HomeMotion.tsx`.
- A readable foreground statement and image sit over two oversized decorative word tracks moving in opposite directions.
- The transition is not JS-pinned. Its existing CSS-sticky viewport must remain readable without motion.
- Do not restore the rejected long pinned ignite treatment.
