# 03 / Effekt — active contract

Last reconciled: 2026-07-16.

- Active files: `src/components/WhatWeImprove.tsx`, `src/styles/what-we-improve.css` and section-scoped motion in `HomeMotion.tsx`.
- Funnet, Forstått, Valgt and Målt remain visible in semantic order as one continuous asymmetric editorial result chain.
- Every outcome retains its explanation, measurement signal and tools in server-rendered HTML.
- Desktop alternates word, proof surface and copy across the grid; tablet and mobile preserve the same left/right rhythm while stacking the four outcomes.
- Osmo Highlight Marker is the active motion: dark covers withdraw from FUNNET, FORSTÅTT, VALGT and MÅLT in two visible rows. Copy, measurement UI and proof surfaces remain stationary.
- The reveal is one-shot and section-scoped. Reduced motion and no-JS use the finished static composition; there is no pin or scrubbed Proof Lock.
- The current mockup surfaces are explicit temporary proof placeholders awaiting final user-supplied assets.
- The section now continues into the unnumbered `03 → 04 / Fra resultat til form` bridge. Its server-rendered thesis is `Effekt må bygges inn.`, connecting the result system to future-facing capability.
- Status: the asymmetric result chain remains approved; the later Highlight Marker replaced the non-running Proof Lock implementation in commit `a1216e2`.
