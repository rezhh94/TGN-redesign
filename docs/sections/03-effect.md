# 03 / Effekt — active contract

Last reconciled: 2026-07-18. Approved commit: `c349ef7`.

- Files: `WhatWeImprove.tsx`, `what-we-improve.css`, `effectScene` and the
  effect states of the mounted global Intro-through-Arbeid `HomeAtmosphere`.
- Funnet, Forstått, Valgt and Målt remain in semantic order with their
  explanations and measurement signals.
- Semantic type is active; visible meta never falls below 10 px.
- The global atmosphere owns the effect/work states
  `entry → effect-focus → handoff → work-focus → exit`.
- Desktop may use existing wave/grain. Compact through 900 px and reduced
  motion use readable static/lightweight variants.
- Current proof mockups remain temporary placeholders.
