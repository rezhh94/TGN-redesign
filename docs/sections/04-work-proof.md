# 04 / Arbeid — active contract

Last reconciled: 2026-07-21. Current implementation is uncommitted.

- Mounted files: `WorkProof.tsx`, `work-proof.css` and `workFocusScene` in
  `HomeMotion.tsx`. `WhatWeImprove.tsx` ends as a clean typographic Effekt
  scene without an outgoing image bridge.
- `Dette kan Tigon lage.` frames six future-facing capabilities. This section
  is never portfolio, customer work or case proof.
- This semantic rule is permanent: never replace it with a list of websites,
  clients or projects Tigon has delivered.
- Webapp, Nettsted, Plattform, E-handel, AI and App are six real, accessible
  direct links with their established hrefs.
- The 03→04 handoff is typography-led. The two title lines enter from opposite
  sides while Arbeid rises on the same dark surface, meet as a complete
  `Dette kan Tigon lage.`, then cross away.
- The title traversal is a separate scrubbed trigger adapted from Trionn's
  verified first-party title module. It is not fused with Trionn's unrelated
  WebGL orbit or its `6.5` pin duration.
- Desktop uses one pinned focus stage after that handoff. The first panel's
  actual Webapp media is initially a central slit, opens into its final
  image-right geometry, and only then yields to the Webapp copy. The image is
  not duplicated in Effekt or in a separate bridge component.
- Six direct-link capability panels replace one another in one stable
  image-right/copy-left composition. The active count and name update with
  scroll and reverse scroll. The local focus pin uses `start: top top`,
  `anticipatePin: 1`, `scrub: .6`, six viewport lengths, panel transition
  `.45` and offset `.2`.
- At 900 px and below, the complete title and explanation lead a normal-flow
  document; each capability is a full-height chapter with its own visible
  action. Reduced motion and no-JS expose the same full content without
  pinning or hidden links.
- Every capability image is decorative, lazy-loaded and paired with visible
  semantic text inside the direct link. Keyboard focus can select an otherwise
  off-screen desktop panel without breaking the six-link tab order.
- No Three.js, TSL, WebGPU, dynamic cursor, title burial, duplicated aperture,
  decorative line field or zig-zag portfolio wall is mounted.
- Layout, component structure and interaction are fully open. Visual freedom
  never permits portfolio or customer-work framing.
