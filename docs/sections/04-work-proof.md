# 04 / Arbeid — active contract

Last reconciled: 2026-07-19. Mounted baseline commit: `af6f28f`.

- Mounted files: `WorkProof.tsx`, `work-proof.css`, `workArchiveScene` and the
  current functional cursor in `HomeMotion.tsx`.
- `Dette kan Tigon lage.` frames six future-facing capabilities. This section
  is never portfolio, customer work or case proof.
- This semantic rule is permanent: never replace it with a list of websites,
  clients or projects Tigon has delivered.
- Webapp, Nettsted, Plattform, E-handel, AI and App are six real, accessible
  direct links with their established hrefs.
- Desktop sticky archive uses clip/scale entry, calibrated local image parallax
  and word-wise title exit on its solid section surface.
- Through 1000 px, touch, reduced motion and no-JS use normal flow. Touch keeps
  a visible `Utforsk` action.
- The current Dynamic Text Cursor is fine-pointer/reduced-motion-safe. A
  redesign may replace or remove it.
- Layout, component structure and interaction are fully open. Visual freedom
  never permits portfolio or customer-work framing.
