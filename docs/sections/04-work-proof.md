# 04 / Arbeid — active contract

Last reconciled: 2026-07-16.

- Active files: `src/components/WorkProof.tsx`, `src/styles/work-proof.css` and section-scoped motion in `HomeMotion.tsx`.
- `Dette kan Tigon lage.` frames six future-facing capabilities in an always-open zig-zag catalogue.
- The `04 / Arbeid` introduction and complete capability wall continue on the same `mauve-warm` surface as Effekt and the 03→04 bridge. There is no white or lighter intermediary surface.
- This is not a portfolio, case list, client archive or proof of previously delivered work.
- Webapp, Nettsted, Plattform, E-handel, AI and App are complete server-rendered surfaces in ordinary flow.
- Each whole surface is one accessible direct link to its established service/guide URL. Desktop cursor text uses `Utforsk / [capability]`; touch/mobile shows `Utforsk` on the surface.
- The former detail dialog, bottom sheet, card-opening GSAP Flip, opening Pixelate and next/previous switcher are removed and must not be restored unless explicitly reopened.
- The 03→04 bridge and Work share one wrapper and one semantic `Dette kan Tigon lage.` node. The same node rotates in during the MWG 053-inspired flip, then contracts into a restrained central watermark behind Webapp through App.
- Desktop presents three asymmetric pairs in sequence: Webapp + Nettsted, Plattform + E-handel, and AI + App. Each pair receives roughly one viewport and moves as a coordinated left/right chapter, keeping the central title legible and avoiding three dominant surfaces at once.
- There is no duplicate Work title. The former `Nettsider, apper og digitale systemer …` interstitial and the repeated footer title are removed so all six real links form one uninterrupted sequence.
- MWG 051 contributes only the foreground-media-over-fixed-message principle. Random positioning, infinite source swapping, Observer and captured wheel/touch input are not used. The three pairs use small deterministic scrubbed settling; no image is cloned, clipped or detached from its real link.
- The section ends with an opaque viewport-height `Slik blir det til.` cover, a Retning / Bygg / Live index and a three-row Osmo Shutter Scroll Transition into 05 / Prosess. The shared wrapper clips its sticky layer at the section boundary. Reduced motion uses the static boundary.
- Do not restore `Selected systems`, the tilted card fan/collage or the removed `WorkShowcase` implementation.
