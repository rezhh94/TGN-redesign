# Kontakt / footer — active contract

Last reconciled: 2026-07-18.

- Active files: `src/components/ContactFooter.tsx`, `src/styles/contact-footer.css`, `HomeMotion.tsx` and `initFooterParallax()` in `src/lib/osmo-motion.ts`.
- This is the calm conversion close after Manifest.
- Desktop uses the existing footer parallax wrapper and a small one-shot wordmark reveal. Mobile, reduced motion and no-JS keep the complete footer in ordinary document flow.
- Footer layout, typography, motion and component structure are fully open to
  redesign. Keep factual NAP and verified contact destinations correct, and do
  not turn it into an SEO link farm.
- Status: mounted baseline only; not visually protected.
