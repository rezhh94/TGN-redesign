import { CharBlob } from "@/components/CharBlob";

/* 02 / Tjenester — "Hva vi bygger". Masthode med en roterende ASCII-tegn-blob
   (signatur-visual, reagerer på kategori-brikkene), deretter tre tjeneste-
   kolonner (Bygg / System / Synlighet). All tekst + internlenker er SSR; bloben
   er et dekorativt, pagespeed-trygt klientlag (Canvas 2D, ingen WebGL). */

const columns = [
  {
    id: "bygg",
    tag: "Bygg",
    items: [
      { name: "Nettsider", href: "/tjenester/webutvikling-nextjs" },
      { name: "Webapper", href: "/tjenester" },
      { name: "Apper", href: "/tjenester/app-utvikling" },
      { name: "E-handel", href: "/tjenester/e-handel-losninger" },
      { name: "Headless CMS", href: "/tjenester/headless-cms" },
      { name: "UX/UI-design", href: "/tjenester/ux-ui-design" },
    ],
  },
  {
    id: "system",
    tag: "System",
    items: [
      { name: "AI-systemer", href: "/tjenester/ai-implementering" },
      { name: "Digital infrastruktur", href: "/tjenester/digital-infrastruktur" },
      { name: "Integrasjoner", href: "/tjenester/ai-integrasjon" },
      { name: "Vedlikehold & sikkerhet", href: "/tjenester/vedlikehold-sikkerhet" },
    ],
  },
  {
    id: "synlighet",
    tag: "Synlighet",
    items: [
      { name: "Teknisk SEO", href: "/tjenester/seo-optimalisering" },
      { name: "AI-søk", href: "/tjenester" },
      { name: "Innholdsstruktur" },
      { name: "Lokal synlighet" },
      { name: "Måling" },
    ],
  },
] as const;

export function WhatWeBuild() {
  return (
    <section className="what-build" aria-labelledby="what-build-title">
      <div className="what-build__inner">
        <div className="what-build__mast">
          <div className="what-build__mast-main">
            <p className="what-build__label">Tjenester</p>
            <h2 className="what-build__title" id="what-build-title">
              Bygger.
            </h2>
            <p className="what-build__lead">
              Vi bygger digitale løsninger som blir funnet, forstått og valgt — struktur, fart og
              måling fra første linje.
            </p>

            <nav className="what-build__chips" aria-label="Tjenestekategorier">
              {columns.map((col) => (
                <a
                  className="what-build__chip"
                  href={`#wb-${col.id}`}
                  data-blob-cat={col.id}
                  key={col.id}
                >
                  {col.tag}
                  <span className="what-build__chip-arrow" aria-hidden="true" />
                </a>
              ))}
            </nav>

            <p className="what-build__count">15 tjenester — én produksjon</p>
          </div>

          <div className="what-build__blob">
            <CharBlob className="what-build__blob-canvas" />
          </div>
        </div>

        <div className="what-build__rule" aria-hidden="true" />

        <div className="what-build__bottom" data-build-list>
          <div className="what-build__tldr" data-build-row>
            <p className="what-build__tldr-text">
              Vi bygger mye. Ta en rask prat, så peker vi ut hva som bør gjøres først.
            </p>
            <a className="what-build__cta" href="/kontakt">
              Få gratis analyse
              <span className="what-build__cta-arrow" aria-hidden="true" />
            </a>
          </div>

          <div className="what-build__cols">
            {columns.map((col) => (
              <div className="what-build__col" id={`wb-${col.id}`} data-build-row key={col.id}>
                <p className="what-build__coltag">[ {col.tag} ]</p>
                <ul className="what-build__list">
                  {col.items.map((item) => (
                    <li className="what-build__item" key={item.name}>
                      {"href" in item ? <a href={item.href}>{item.name}</a> : <span>{item.name}</span>}
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}
